services:
  app:
    container_name: ai-tech-service-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8999:80"
    restart: unless-stopped
    networks:
      - apps-network

networks:
  apps-network:
    external: true
